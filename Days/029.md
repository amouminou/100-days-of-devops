# Pull Request Workflow – Fox and Grapes Story

## Objective

Prevent direct pushes to the master branch and merge changes only through a reviewed Pull Request.

## Step 1: SSH into Storage Server as Max

```bash
ssh max@storage-server
```

**Credentials:**
- Password: `Max_pass123`

## Step 2: Navigate to the Cloned Repository

```bash
cd ~
ls
cd <repository-name>
```

## Step 3: Verify Repository Contents

Check that Sarah's story exists:

```bash
ls
```

Review commit history and author information:

```bash
git log
```

**Confirm:**
- Sarah's commits are present
- Author name/email is correct
- Commit messages are valid

## Step 4: Verify Remote Branches

```bash
git branch -r
```

**Expected output includes:**
- `origin/master`
- `origin/story/fox-and-grapes`

## Step 5: Create Pull Request in Gitea

1. Open Gitea UI from the top bar
2. Login as max
   - **Username:** `max`
   - **Password:** `Max_pass123`
3. Navigate to the repository
4. Click **Pull Requests → New Pull Request**
5. Enter PR Details:
   - **Title:** Added fox-and-grapes story
   - **Source Branch:** `story/fox-and-grapes`
   - **Destination Branch:** `master`
6. Click **Create Pull Request**

## Step 6: Assign Reviewer

1. Open the newly created PR
2. On the right panel, click **Reviewers**
3. Add `tom` as a reviewer

## Step 7: Review and Merge as Tom

1. Logout from Gitea (max)
2. Login as tom
   - **Username:** `tom`
   - **Password:** `Tom_pass123`
3. Open the Pull Request: "Added fox-and-grapes story"
4. Review changes
5. Click **Approve**
6. Click **Merge Pull Request**

## Result

✓ Direct pushes to master avoided
✓ Changes merged through PR
✓ Review and approval completed
✓ Master contains approved fox-and-grapes story